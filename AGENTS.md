# 项目维护规则

## 唯一源码入口

- 本目录就是可上传仓库的项目根目录。
- `src/` 与 `public/` 是现役代码和素材，禁止从外层工作区的旧 `src/`、`reference/` 或 `.upstream-import/` 直接构建发布。
- `deploy/` 只保留可复用配置，不存放镜像压缩包、临时截图或服务器数据。

## 修改边界

- 不以“性能优化”为由删除首屏、音乐、头像律动、卡片、滚动揭示或视频动态效果。
- 手机端和电脑端必须同时验证；手机优化不得改变电脑端既有效果。
- 留言数据、音乐会话密钥、环境变量和服务器凭据不得进入 Git。

## 发布前验收

1. `npm ci`
2. `npm run check`
3. `npm run build`
4. 桌面端与 390×844 手机端浏览器验收
5. 检查横向溢出、控制台错误、核心视频播放、主要动画与 `/api/wall`

生产发布必须使用新镜像标签，保留当前版本作为回退点，先经过候选容器验收再切换流量。
